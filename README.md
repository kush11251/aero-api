# Aero API

A high-performance RESTful API for managing airline flight schedules and bookings.

## Getting Started
To run the API, navigate to the project directory and execute the following command:

git clone https://github.com/user/aero-api.git

cd aero-api

npm install
cd src
node index.js
